\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{notasclase}

\RequirePackage[a4paper, total={17cm,27cm}, left=1.5cm, bottom=1.5cm, includehead,%
% showframe
]{geometry}
\RequirePackage{mathtools, amsfonts, amssymb, mathpazo}
\RequirePackage[scaled]{helvet}
\RequirePackage[T1]{fontenc}
\RequirePackage[utf8]{inputenc}
\setlength{\parindent}{1cm}
\setlength{\parskip}{12pt}
\renewcommand{\baselinestretch}{1.25}

\RequirePackage{array, xcolor, colortbl, soul}
\RequirePackage{graphicx}
\RequirePackage{wrapfig, boxedminipage}
\RequirePackage{tabularx}
\RequirePackage{ifthen}
\RequirePackage{multicol, etoolbox}
\setlength{\columnsep}{5mm}


% Formato de section, subsection y subsubsection
\usepackage[explicit]{titlesec}
\titleformat{\section}{\large\sffamily\bfseries}{\thesection}{.5cm}{\MakeUppercase{#1}}[]
\titleformat{\subsection}{\sffamily\bfseries}{\thesubsection}{.5cm}{\MakeUppercase{#1}}[]
\titleformat{\subsubsection}{\sffamily\small\bfseries}{\thesubsubsection}{.5cm}{#1}[]

% Caligrafía
\newcommand{\Prob}[1]{\ifthenelse{\equal{#1}{}}{\mathcal{P}}{\mathcal{P}(#1)}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\NN}{\mathbb{N}}
\newcommand{\va}[1]{\mathbf{#1}}
\newcommand{\vva}[1]{\bar{\mathbf{#1}}}
\newcommand{\vvva}[1]{\bar{\bar{\mathbf{#1}}}}
\newcommand{\Fva}[1]{F_{\va{#1}}}
\newcommand{\fva}[1]{f_{\va{#1}}}
\newcommand{\pva}[1]{p_{\va{#1}}}


% Header y Footer
\global\let\@Titulo\@empty
\def\Titulo#1{\gdef\@Titulo{#1}}
\newcommand{\tituloNotasHeader}{\ifx\@Titulo\@empty \else \textcolor{black}{\@Titulo} \fi}
\newcommand{\headerfont}{\normalfont\sffamily\fontsize{7}{9} \selectfont}

\RequirePackage{fancyhdr}
\pagestyle{fancy}
\setlength{\headheight}{1cm}
\fancyhead{
    \headerfont
    \begin{tabular*}{\textwidth}{c @{\extracolsep{\fill}} c|}
        \arrayrulecolor{black}
        \hline
        \cellcolor{black}\color{white}\textbf{\thepage} & {\tituloNotasHeader} \\
        \hline
    \end{tabular*}
}
\renewcommand{\headrulewidth}{0pt}
\lfoot{}
\cfoot{}
\rfoot{}


% Colores
\definecolor{zukiyellow}{HTML}{F6E9AC}
\definecolor{zukiblack}{HTML}{44342B}
\definecolor{zukiblue}{HTML}{acbdf6}

\newcommand{\hlz}[1]{%
    {%
    \sethlcolor{zukiyellow}%
    \hl{#1}%
    }%
}

% Tcolorboxes
\RequirePackage[most, breakable]{tcolorbox}
\newtcolorbox{apuntesbox}[1]{%
    colback=gray!1!white,
    colframe=black,
    left=1pt,
    grow to right by=5mm,
    grow to left by=-5mm,
    bottom=5mm,
    fonttitle=\bfseries\sffamily,
    title=#1,
    sharp corners,
    breakable}

\newtcolorbox{apuntesfigbox}{%
    colback=gray!1!white,
    colframe=black,
    left=1pt,
    grow to right by=5mm,
    grow to left by=-5mm,
    bottom=5mm,
    fonttitle=\bfseries\sffamily,
    sharp corners,
    title={},
    breakable}

% TikZ
\RequirePackage{tikz, pgfplots, pgfplotstable}
\pgfplotsset{compat=1.18}
\usetikzlibrary{patterns}
\usetikzlibrary{patterns.meta}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{tikzmark}
\usepgfplotslibrary{fillbetween}


% table of contents
\RequirePackage{tocloft}

\setlength{\cftsecindent}{0pt}% Remove indent for \section
\setlength{\cftsubsecindent}{0pt}% Remove indent for \subsection
\setlength{\cftsubsubsecindent}{0pt}% Remove indent for \subsubsection


% gauss shit

% define gaussian pdf and cdf
\pgfmathdeclarefunction{gauss}{3}{%
  \pgfmathparse{1/(#3*sqrt(2*pi))*exp(-((#1-#2)^2)/(2*#3^2))}%
}
\pgfmathdeclarefunction{cdf}{3}{%
  \pgfmathparse{1/(1+exp(-0.07056*((#1-#2)/#3)^3 - 1.5976*(#1-#2)/#3))}%
}
\pgfmathdeclarefunction{fq}{3}{%
  \pgfmathparse{1/(sqrt(2*pi*#1))*exp(-(sqrt(#1)-#2/#3)^2/2)}%
}
\pgfmathdeclarefunction{fq0}{1}{%
  \pgfmathparse{1/(sqrt(2*pi*#1))*exp(-#1/2))}%
}

\colorlet{mydarkblue}{blue!30!black}
\usetikzlibrary{arrows.meta}
\usepgfplotslibrary{groupplots}
\tikzset{>=stealth,
OptimumStyle/.style={align=center,anchor=east,rotate=90,font=\sffamily\scriptsize}
}

\def\N{50}

\endinput